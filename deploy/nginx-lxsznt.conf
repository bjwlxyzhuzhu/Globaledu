# 寰语星球 · lxsznt.ujs.edu.cn 反向代理
# 放置位置：/etc/nginx/conf.d/lxsznt.conf （或 sites-available + 软链）
# 应用容器监听 127.0.0.1:8787，由 Nginx 对外提供 80/443。

server {
    listen 80 default_server;
    server_name lxsznt.ujs.edu.cn;

    # 拿到证书前先直接代理；配好 HTTPS 后把下面两行注释掉、启用 443 段并打开跳转。
    # return 301 https://$host$request_uri;

    client_max_body_size 32m;

    location / {
        proxy_pass http://127.0.0.1:8787;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";

        # LLM 流式输出：关闭缓冲并放宽超时
        proxy_buffering off;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}

# 学校下发证书后启用：
# server {
#     listen 443 ssl http2;
#     server_name lxsznt.ujs.edu.cn;
#
#     ssl_certificate     /etc/nginx/ssl/lxsznt.ujs.edu.cn.crt;
#     ssl_certificate_key /etc/nginx/ssl/lxsznt.ujs.edu.cn.key;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     client_max_body_size 32m;
#
#     location / {
#         proxy_pass http://127.0.0.1:8787;
#         proxy_http_version 1.1;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade           $http_upgrade;
#         proxy_set_header Connection        "upgrade";
#         proxy_buffering off;
#         proxy_read_timeout 300s;
#         proxy_send_timeout 300s;
#     }
# }

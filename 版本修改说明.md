# 国际教育智能体平台：版本文件说明

## 版本信息

- GitHub 仓库：`bjwlxyzhuzhu/Globaledu`
- 当前分支：`master`
- 当前提交：`98a66e5f92504204b35243db7efee1006277b658`
- 当前提交标题：`Initial import of international education agent platform`
- 当前仓库共纳入 161 个受 Git 管理的文件。

## 说明口径

当前仓库只有一次 Git 提交，未保留更早版本的提交记录。因此，本文件可以说明当前上传版本包含哪些文件和功能，但不能在没有旧版源码、旧版压缩包或旧 Git 提交的情况下，严谨判断每个文件相对旧版的逐行差异。

## 主要文件和修改范围

### 1. 页面与业务入口：`app/`

- `app/page.tsx`：平台首页入口。
- `app/login/page.tsx`：学生和管理员登录页面。
- `app/apply/`：创新创业应用中心，包括：
  - `cockpit/page.tsx`：项目驾驶舱。
  - `crew/page.tsx`：智能体团队工作区。
  - `defense/page.tsx`：项目答辩与防守训练。
  - `expert/page.tsx`：专家指导入口。
  - `ppt/page.tsx`：项目演示文稿生成入口。
  - `skills/page.tsx`：能力训练模块。
  - `text/page.tsx`：项目文本生成与润色。
  - `topic/page.tsx`：选题与项目方向入口。
  - `CoachSession.tsx`、`RadarChart.tsx`：辅导会话和能力雷达图。
- `app/learn/`：学习中心，包括案例、政策、理论、模板和链接模块。
- `app/admin/`：管理员入口页面。
- `app/compliance/page.tsx`：合规与隐私说明页面。
- `app/globals.css`、`app/layout.tsx`：全局样式、字体和页面布局。

### 2. 服务端 API：`app/api/`

- `app/api/ai/`：AI 问答、聊天、文件处理、文本润色和语音合成。
- `app/api/admin/`：管理员操作，包括知识库、学生、名册、超星数据、效果配置和研究成果导出。
- `app/api/credits/`：积分查询和每日积分。
- `app/api/evidence/`：证据材料处理。
- `app/api/interventions/`：干预记录和辅导动作。
- `app/api/ppt/generate/`：项目演示文稿生成。
- `app/api/projects/`：项目数据管理。
- `app/api/stats/`：平台统计数据。

### 3. 前端组件：`components/`

- `GalaxyHome.tsx`、`BlackHole.tsx`、`WarpStarfield.tsx`、`galaxyEngine.js`：平台首页的银河探索视觉界面。
- `AccountPanel.tsx`、`LoginModal.tsx`、`AccountButton.tsx`：登录、账户和个人信息交互。
- `CrewWorkspace.tsx`、`CrewStage.tsx`、`SkillPicker.tsx`、`ModelSelect.tsx`：智能体团队和模型选择界面。
- `ChartBlock.tsx`、`LiveWall.tsx`、`DailyBonus.tsx`：数据展示、动态墙和积分相关组件。
- `components/admin/`：管理员控制台、知识库、名册导入、学生管理、干预管理、效果配置和使用统计组件。

### 4. 业务逻辑与数据访问：`lib/`

- `lib/ai/`：AI 模型、供应商、请求解析、联网搜索和网页抓取。
- `lib/auth.ts`：登录和会话处理。
- `lib/credits.ts`：积分扣除、查询和使用记录。
- `lib/contest.ts`：竞赛项目相关逻辑。
- `lib/crew.ts`、`lib/skills.ts`：智能体团队和训练技能逻辑。
- `lib/evidence.ts`、`lib/interventions.ts`：证据和辅导干预逻辑。
- `lib/download.ts`、`lib/extract.ts`：文件下载和内容提取。
- `lib/supabase/`：Supabase 浏览器端、服务端和管理员端客户端。

### 5. 数据库迁移：`supabase/migrations/`

包含 0001 至 0011 的数据库迁移，覆盖用户资料、API 密钥、学习中心 RAG、管理员权限、项目、每日积分、技能、证据、干预、统计和对抗式对话等功能；`_ALL.sql` 用于汇总迁移内容。

### 6. 素材与视觉资源：`public/`

- 驾驶舱背景和场景图：`public/cockpit*`。
- 智能体角色图：`public/crew/`。
- 人物和品牌素材：`public/human-fire.jpg`、`public/human-water.jpg`、`public/logo.svg`。

### 7. 部署和运行配置

- `package.json`、`pnpm-lock.yaml`：Next.js、React、Supabase、AI、文件处理和图表相关依赖。
- `Dockerfile`、`docker-compose.yml`：容器化部署配置。
- `middleware.ts`、`instrumentation.ts`：会话刷新和运行时初始化。
- `scripts/`：数据库迁移、数据导入、知识库处理、素材生成、服务启动和技能测试脚本。
- `.env.example`：环境变量模板。
- `.env.local` 未纳入 Git，服务端密钥不应提交到仓库。

## 可直接转发的简要说法

本次上传的是国际教育智能体平台的完整 Web 版本，主要包括首页银河交互界面、登录与账户体系、学习中心、创新创业应用中心、智能体团队工作区、项目驾驶舱、AI 问答与文本生成、PPT 生成、管理员后台、知识库与名册管理、积分系统、证据和干预记录，以及 Supabase 数据库迁移和 Docker 部署配置。由于当前 GitHub 仓库是首次导入，仓库内没有旧版本提交记录，所以暂时只能提供当前版本的文件范围说明；要生成严格的“旧版/新版修改清单”，还需要旧版代码或旧版提交作为对比基准。

// 数据灌库脚本占位（M5+ 实现）：把 /data 下的种子写入 SQLite。
// 目前 M1/M2 直接读取 /data 下的 JSON，无需灌库。
console.log(
  '[seed] 暂未实现：M1/M2 阶段直接读取 /data 下 JSON，无需灌库。\n' +
    '       M5 起在此把 汉字 / HSK 词表 / 知识库 写入 node:sqlite（含向量化）。',
);

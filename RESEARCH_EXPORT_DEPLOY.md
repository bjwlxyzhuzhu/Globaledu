# 研究数据导出功能部署说明

已新增管理员研究数据导出功能。

## 使用

管理员登录 `/admin`，选择班级，在花名册区域点击“导出研究数据”。浏览器会下载一个 Excel 文件，包含匿名学生信息、学习记录、写作记录、对话消息、积分事件和能力评分等工作表。

## 部署

Docker 部署：

```bash
docker compose up -d --build
```

单进程部署：

```bash
pnpm install
pnpm serve
```

## 说明

导出的研究数据使用 C001、C002 等匿名编号，不导出姓名、学号和密码。管理员原有的花名册 Excel/PDF 导出保持不变。